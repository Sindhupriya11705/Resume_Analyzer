[project]
name = "skillforge-ai"
version = "0.1.0"
description = "SkillForge AI scoring and recommendation engine"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

